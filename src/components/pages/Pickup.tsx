import { Layout, PageHeader } from "antd";
const { Content } = Layout;

const Pickup = () => {
  return (
    <Layout>
      <Content>
        <PageHeader title="Пункты выдачи" />
      </Content>
    </Layout>
  );
};

export default Pickup;
